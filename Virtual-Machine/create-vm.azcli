# Creacion de una maquina virtual.

az vm create \
    --resource-group RG-Networking-Lab \
    --name VM-FrontEnd \
    --location eastus \
    --image Ubuntu2204 \
    --size Standard_B1s \
    --admin-username cloudadmin \
    --nics NIC-FrontEnd \
    --generate-ssh-keys
